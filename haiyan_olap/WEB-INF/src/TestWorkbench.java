public class TestWorkbench {

	public static void main(String[] args) {
		// try {
		// mondrian.gui.Workbench w = new Workbench();
		// w.parseArgs(args);
		// w.setSize(800, 600);
		// // if user specified a file to open, do so now.
		// if (w.openFile != null) {
		// File f = new File(w.openFile);
		// if (f.canRead()) {
		// w.openSchemaFrame(f.getAbsoluteFile(), false); // parameter
		// // to
		// // indicate
		// // this is a
		// // new or
		// // existing
		// // catalog
		// // file
		// }
		// }
		// w.show();
		// } catch (Throwable ex) {
		// // LOGGER.error("main", ex);
		// }
	}
}
